<!-- K-라이스페스타 품평회 시스템의 비즈니스 로직 및 계산 규칙 정리 문서 -->
# 품평회 평가 시스템 핵심 비즈니스 로직 정리

본 문서는 K-라이스페스타 품평회 평가 시스템 개발을 위한 핵심 계산 로직 및 동기화 방식을 정리한 문서입니다.

---

## 1. 평가 방식 분류

품평회는 등록된 부문에 따라 두 가지 방식으로 자동 분기됩니다.

### 1) 오픈 테스트 (쌀가공식품 부문)
* 대상 부문 예시: 조리 (A), 비조리 (B), 농협 (C)
* 데이터 특징: 평가자에게 제품명과 분류코드가 모두 공개됩니다.
* 배점 방식: 관능 평가(100점 만점)와 상품성 평가(50점 만점)를 병합하여 사용합니다.

### 2) 블라인드 테스트 (우리술 부문)
* 대상 부문 예시: 저도발효주 (가), 고도발효주 (나), 약·청주 (다), 증류주 (라)
* 데이터 특징: 평가자에게 제품명은 숨겨지고 오직 분류코드(예: 가-1)만 노출됩니다.
* 배점 방식: 상품성 평가 없이 관능 평가(120점 만점) 5개 항목만 사용합니다.

---

## 2. 점수 계산 및 집계 규칙

### 1) 오픈 테스트 점수 환산 및 소계
* **관능 평가 항목**:
  * 식품의 색(15점), 식품의 향(15점), 식품의 맛(30점), 식품의 식감(20점), 종합평가(20점) = 합계 100점 만점.
  * 최종 계산 시 관능 평가 점수는 **70점 만점으로 자동 환산**됩니다.
  * 환산 공식: `관능 평가 총점 × 0.7`
* **상품성 평가 항목**:
  * 창의성(30점), 디자인(20점) = 합계 50점 만점.
  * 환산 없이 원점수를 그대로 반영합니다.
* **최종 소계 계산**:
  * `소계 = (관능 평가 총점 × 0.7) + 상품성 평가 총점` (최대 120점 만점)

### 2) 블라인드 테스트 점수 소계
* **관능 평가 항목**:
  * 술의 색(20점), 술의 향(20점), 술의 맛(30점), 후미 및 목넘김(20점), 종합평가(30점) = 합계 120점 만점.
* **최종 소계 계산**:
  * 환산 없이 5개 관능 평가 항목의 점수를 그대로 합산합니다.
  * `소계 = 관능 평가 총점` (최대 120점 만점)

### 3) 관리자 결과 집계 (최고/최저점 제외 순위 산정)
* 각 제품별로 모든 심사위원이 매긴 최종 소계(120점 만점 기준)들을 집계합니다.
* **최고/최저 제외 로직**:
  * 특정 제품에 대해 수집된 심사위원 점수 중 **가장 높은 점수 1개와 가장 낮은 점수 1개를 집계에서 제외**합니다.
  * 제외된 점수를 뺀 나머지 점수들의 평균 또는 합계를 '최종 합계'로 산정합니다.
  * 심사위원이 3명 이상일 때에만 이 규칙이 작동합니다.
* **순위 결정**:
  * 계산된 최종 합계를 기준으로 내림차순 정렬하여 순위를 자동으로 매깁니다.

---

## 3. 데이터 저장 및 동기화 무결성 규칙 (구현 반영)

유저앱은 오프라인 우선(offline-first)으로 동작하며, 로컬 저장은 IndexedDB(Dexie)를 사용합니다. 구현 위치는 `web-user/src/db.js`입니다.

### 1) 로컬 DB 구조 (Dexie: DB명 `kricefesta`)
* `meta` — 대회 메타 캐시. 키 `groupName`. `/init` 응답(systemName·부문·제품·템플릿·평가자 명단)을 통째로 저장하여 오프라인 진입에 사용합니다.
* `scores` — 점수 셀. 복합 기본키 `[groupName+judgeName+bumanKey+productCode+itemId]`로 중복을 방지하고, 보조 인덱스 `[groupName+judgeName]`(조회)과 `syncStatus`(동기화)를 둡니다.
* `session` — 대회별 현재 평가자 세션. 키 `groupName`. 평가자명·judgeId·부문 완료 상태를 보관합니다.

### 2) 중복 저장 방지
* 동일 평가자가 같은 제품의 같은 항목을 재입력하면 복합키가 같아 덮어써지고 중복 행이 생기지 않습니다(Dexie `put`).
* 항목 단위(itemId)로 저장하므로 로직정리 원안의 `[groupId+judgeName+bumanKey+productCode]`보다 한 단계 세분화된 셀 단위 관리입니다.

### 3) 오프라인 진입·검증
* 접근: 마운트 시 `/init`을 시도해 성공하면 상태 반영 후 `meta`에 캐시하고 진입 허용. 네트워크 오류(오프라인)면 `meta` 캐시가 있을 때만 캐시로 진입 허용, 없으면 차단. 서버가 응답했으나 대회 없음(잘못된 코드)이면 차단.
* 로그인: 온라인이면 서버가 등록 평가자를 검증(미등록 404). 오프라인이면 캐시된 평가자 명단(`meta.judges`)에서 이름을 검증하고, 없으면 진입 차단.

### 4) 점수 저장 및 동기화(sync_status)
* 점수 입력 시 먼저 로컬 `scores`에 `syncStatus: 'pending'`으로 저장합니다(오프라인에서도 보존).
* 이어서 서버 `/score`로 전송을 시도하고, 성공 응답을 받으면 해당 셀을 `syncStatus: 'synced'`로 전환합니다.
* 로그인 직후와 브라우저 `online` 이벤트 발생 시 `pending` 셀을 모아 서버로 재전송하고 성공분을 `synced` 처리합니다.
* 로그인 시 화면 점수의 최종 소스는 로컬 `scores`이며, 온라인이면 서버 복원 점수를 로컬(`synced`)로 먼저 반영한 뒤 로컬을 로드해 오프라인 입력분과 병합합니다.

## 4. 기기 인증(전용 태블릿 등록/승인) — 구현 반영

URL만 알면 데이터가 노출되던 문제를 막기 위해, 승인된 기기만 대회 데이터를 받고 채점할 수 있도록 기기 인증을 도입했습니다.

### 1) 데이터 모델
* `fair_device` — 대회별 기기. device_key(브라우저별 UUID), label(기기 이름), status(pending/approved/blocked), auth_key(승인 시 발급 토큰), last_seen. (fg_id+device_key 유니크)
* `fair_group.fg_enroll_open` — 신규 기기 등록 허용/잠금 플래그.

### 2) 흐름
1. 유저앱 접속 시에는 IndexedDB의 device_key(최초 1회 생성)로 `/device/status`만 호출합니다. 이 조회는 서버에 저장하지 않으므로, URL만 아는 기기가 무단으로 쌓이지 않습니다. 미등록이면 unregistered로 응답합니다.
2. 사용자가 기기 이름을 입력하고 "기기 등록 요청"을 눌렀을 때만 `/device/register`로 대기(pending) 저장합니다(등록잠금이면 거부).
2. 미승인 기기는 "기기 인증 필요" 화면만 보이고 `/init` 데이터는 받지 못합니다. 사용자는 기기 이름을 입력해 등록을 요청합니다.
3. 관리자 콘솔 "기기 관리"에서 승인하면 서버가 무작위 인증키(auth_key)를 발급합니다.
4. 승인된 기기는 `/init`·`login`·`score`·`complete` 호출 시 헤더 `X-Device-Key`·`X-Auth-Key`를 함께 보내고, 서버는 매 요청마다 device_key+auth_key가 승인 상태 행과 일치하는지 재확인합니다. 관리자가 승인을 끄면 즉시 차단됩니다.

### 3) 오프라인
* 승인된 기기는 인증키와 대회 캐시를 로컬에 보관하므로, 온라인 재확인이 안 되는 오프라인에서도 캐시된 인증 상태로 채점을 계속하고, 복귀 시 pending 점수를 동기화합니다.
* 기기-평가자는 묶지 않습니다. 승인된 기기에서 평가자는 이름으로 로그인하며 교체될 수 있습니다.

### 4) 남은 강화 여지
* PIN(2차 인증), CORS 특정 도메인 제한. 기기키/인증키는 브라우저에 저장되어 F12로 읽힐 수 있으므로, 보안 강도는 승인된 기기의 물리적 통제에 의존합니다.
