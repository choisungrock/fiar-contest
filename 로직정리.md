<!-- K-라이스페스타 품평회 시스템의 비즈니스 로직 및 계산 규칙 정리 문서 -->
# 품평회 평가 시스템 핵심 비즈니스 로직 정리

본 문서는 K-라이스페스타 품평회 평가 시스템 개발을 위한 핵심 계산 로직 및 동기화 방식을 정리한 문서입니다.

---

## 1. 평가 방식 분류

품평회는 등록된 부문에 따라 두 가지 방식으로 자동 분기됩니다.

### 1) 오픈 테스트 (쌀가공식품 부문)
* 대상 부문 예시: 조리 (A), 비조리 (B), 농협 (C)
* 데이터 특징: 평가자에게 제품명과 분류코드가 모두 공개됩니다.
* 배점 방식: 관능 평가(100점 만점)와 상품성 평가(50점 만점)를 병합하여 사용합니다.

### 2) 블라인드 테스트 (우리술 부문)
* 대상 부문 예시: 저도발효주 (가), 고도발효주 (나), 약·청주 (다), 증류주 (라)
* 데이터 특징: 평가자에게 제품명은 숨겨지고 오직 분류코드(예: 가-1)만 노출됩니다.
* 배점 방식: 상품성 평가 없이 관능 평가(120점 만점) 5개 항목만 사용합니다.

---

## 2. 점수 계산 및 집계 규칙

### 1) 오픈 테스트 점수 환산 및 소계
* **관능 평가 항목**:
  * 식품의 색(15점), 식품의 향(15점), 식품의 맛(30점), 식품의 식감(20점), 종합평가(20점) = 합계 100점 만점.
  * 최종 계산 시 관능 평가 점수는 **70점 만점으로 자동 환산**됩니다.
  * 환산 공식: `관능 평가 총점 × 0.7`
* **상품성 평가 항목**:
  * 창의성(30점), 디자인(20점) = 합계 50점 만점.
  * 환산 없이 원점수를 그대로 반영합니다.
* **최종 소계 계산**:
  * `소계 = (관능 평가 총점 × 0.7) + 상품성 평가 총점` (최대 120점 만점)

### 2) 블라인드 테스트 점수 소계
* **관능 평가 항목**:
  * 술의 색(20점), 술의 향(20점), 술의 맛(30점), 후미 및 목넘김(20점), 종합평가(30점) = 합계 120점 만점.
* **최종 소계 계산**:
  * 환산 없이 5개 관능 평가 항목의 점수를 그대로 합산합니다.
  * `소계 = 관능 평가 총점` (최대 120점 만점)

### 3) 관리자 결과 집계 (최고/최저점 제외 순위 산정)
* 각 제품별로 모든 심사위원이 매긴 최종 소계(120점 만점 기준)들을 집계합니다.
* **최고/최저 제외 로직**:
  * 특정 제품에 대해 수집된 심사위원 점수 중 **가장 높은 점수 1개와 가장 낮은 점수 1개를 집계에서 제외**합니다.
  * 제외된 점수를 뺀 나머지 점수들의 평균 또는 합계를 '최종 합계'로 산정합니다.
  * 심사위원이 3명 이상일 때에만 이 규칙이 작동합니다.
* **순위 결정**:
  * 계산된 최종 합계를 기준으로 내림차순 정렬하여 순위를 자동으로 매깁니다.

---

## 3. 데이터 저장 및 동기화 무결성 규칙

### 1) 태블릿 로컬 DB 중복 저장 방지
* 로컬 DB(IndexedDB)에 임시 저장 시 데이터 무결성을 보장하기 위해 기본 키를 복합 키로 구성합니다.
* **기본 키 구성**: `[groupId + judgeName + bumanKey + productCode]`
* 동일한 평가자가 같은 제품에 대해 점수를 재입력할 경우 데이터가 덮어씌워지며 중복 행이 생기지 않도록 관리합니다.

### 2) 서버 전송 검증 절차
* 태블릿 클라이언트 데이터 상태는 `sync_status` 플래그로 구분됩니다.
* **저장 확인 흐름**:
  1. 평가 제출 버튼 클릭 시 로컬 데이터를 서버 API로 전송하고 로컬은 전송 대기 상태를 유지합니다.
  2. 서버는 DB 트랜잭션을 실행하여 영속적 저장이 완료되면 성공 코드를 반환합니다.
  3. 클라이언트는 성공 응답을 완벽히 수신한 직후에만 `sync_status`를 `completed`로 전환하고 화면에 저장 완료(초록 체크)를 표시합니다.
